# DISCLAIMER

### Again, PLEASE do not use it on other networks.

#### If you want to test something like a custom WAF or firewall, PLEASE do not use python.
#### As its really slow for those kind of things.

------------------------------------------------------------------------------------------

`_http.py`:
* Usage: _http.py `<url>` `<port>` `<duration>` `<thread>`

This tool sends raw HTTP/1.1 request using socket, and threading to make rapid requests for a HTTP web server ( NO PROTECTION / TLS )

------------------------------------------------------------------------------------------
